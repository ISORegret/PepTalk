# React Native 0.81 bundles react-native-renderer@19.1.0, which requires react@19.1.0 exactly.
# expo-router pulls in react-dom@19.2.4 as optional, which wants react@^19.2.4.
# We use react@19.1.0 to match the renderer; this flag avoids install failures on the peer mismatch.
legacy-peer-deps=true
