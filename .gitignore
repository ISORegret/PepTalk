# Env (Supabase keys, etc.)
.env
.env.*
!.env.example

# Optional Supabase client config for static hosting (create from supabase-config.example.json).
# GitHub Actions can generate this from secrets before build; commit with -f if you prefer it in-repo.
public/supabase-config.json

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
